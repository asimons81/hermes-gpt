# v0.7.0 Flight Deck — Implementation Evidence Report

- Task: t_d31703e4 (Isolated Implementation and Tests)
- Date: 2026-08-15
- Owner: hermes-dev
- Inputs: architecture (t_5a3f6b5c), legal risk review (t_164cf992),
  interaction design (t_dd366ab6)
- Repository: /home/tony/hermes-gpt @ master (local commits, not pushed)

## Summary

All implementation slices S0–S9 from the architecture implementation plan were
implemented as isolated, reviewable commits. The full test suite is green
(643 passed, 3 skipped; CI-like environment without Hermes Agent source tree).
Release doctor is WARN-only (blocking cleared). Package hygiene guard passes
on wheel + sdist.

## Slice-by-slice evidence

### S0 — CI fix (hermes_constants import hermeticity)
- Commit: `68e3397`
- Files: `operator_skills.py`, `test_operator_skills.py`
- Change: `_call_skill_manager` degrades gracefully when `hermes_constants`
  is unavailable — for the default profile (profile_home == hermes_root),
  scoping is a no-op so the manager call proceeds; for a non-default profile
  it fails closed rather than writing to the wrong profile. The
  profile-scoping test uses `pytest.importorskip("hermes_constants")` so it
  runs only where the Hermes tree is present (make-tests-hermetic alternative).
- Verified: `test_operator_skills.py` 34/34 pass with Hermes tree; 33 pass +
  1 skip without (CI-like). Previously 8 failures.

### S1 — MCP compatibility manifest + tests
- Commit: `575b388`
- Files: `docs/mcp-compatibility.md` (new), `test_mcp_compat.py` (new),
  `README.md`, `docs/README.md`
- Change: pins minimum protocol 2024-11-05 through SDK latest 2025-11-25,
  transport matrix, auth metadata; tests assert SDK floor, initialize
  negotiation, and securitySchemes per auth configuration.
- Verified: `test_mcp_compat.py` 9/9 pass.

### S2 — Restart reconciliation + idempotent advance
- Commit: `6f49085`
- Files: `operator_recovery.py` (new), `test_operator_recovery.py` (new),
  `operator_swarm.py`, `server.py`, `pyproject.toml`
- Change: `hermes_operator_reconcile` / server `hermes_swarm_reconcile`
  marks swarm stages stuck in `running` as `blocked` with
  `reason: interrupted_by_restart`, never auto-advances, reloads the token
  envelope (when S5 present). `hermes_swarm_stage_advance` is idempotent for
  already-done/validated stages (returns current state, `idempotent: true`).
- Verified: `test_operator_recovery.py` 7/7; `test_operator_swarm.py`
  unchanged green; reconcile apply requires workspace+direct policy.

### S3 — Review-accept writer
- Commit: `1d94a9a`
- Files: `operator_review.py` (new), `test_operator_review.py` (new),
  `operator_contract.py`, `server.py`, `pyproject.toml`
- Change: `hermes_review_accept` (owner + direct + confirm) writes
  ReviewAcceptanceRecord JSONL at
  `<hermes_data>/review-evidence/review-acceptances.jsonl` (0600, append-only);
  distinct reviewer enforced at write + validate; verdict vocabulary bounded;
  evidence referenced not copied; denied-path refs rejected (legal L4).
  `operator_contract._check_review` reads the store as evidence 1b.
- Verified: `test_operator_review.py` 12/12 adversarial (self-review rejected
  at write and validate, verdict bounded, evidence required, owner gate,
  confirm gate, dry-run, audit, store read by validator, human-approval path
  preserved).

### S4 — Structured event history surface
- Commit: `d456eb9`
- Files: `operator_events.py` (new), `test_operator_events.py` (new),
  `server.py`, `pyproject.toml`
- Change: `hermes_events_query` / `hermes_events_tail` read-model over audit
  JSONL, swarm-workflows, codex-jobs, cron executions.db, kanban task_events;
  allowlist `HERMES_GPT_EVENTS_ALLOWED_SOURCES` (unset=all/list/empty=none),
  retention `HERMES_GPT_EVENTS_MAX_AGE_DAYS` (default 90), bounded caps + TTL
  cache, Mission-Control redaction invariants, read-only by construction.
- Verified: `test_operator_events.py` 12/12 (no-raw-body, allowlist
  semantics, retention, subject/kind filters, caps/truncation, read-only,
  audit wiring).

### S5 — Durable encrypted token storage + OAuth promotion
- Commit: `f462dda`
- Files: `token_store.py` (new), `operator_oauth.py` (new),
  `test_token_store.py` (new), `oauth_auth.py`, `operator_policy.py`,
  `server.py`, `pyproject.toml`, `MANIFEST.in`, `docs/oauth.md`,
  `test_package_hygiene.py`
- Change: AES-256-GCM envelope at
  `<hermes_data>/secrets/hermes_gpt_tokens.json` (0600); key precedence
  keyring → key file (0600) → env `HERMES_GPT_TOKEN_MASTER_KEY`; rotation via
  kid; `hermes_oauth_status` (read-only, presence/expiry only) and
  `hermes_oauth_revoke` (owner + confirm); OAuthState persist/restore +
  server persist hook; `secrets/` added to denied dir names (ADR-006);
  `cryptography` required dep + `keyring` dev dep.
- Verified: `test_token_store.py` 13/13 (ciphertext-on-disk, 0600,
  restart roundtrip, corrupt fail-closed, revoke+rotate, env key, no token
  material on status, owner gate, dry-run, persist hook, restore);
  `test_oauth_auth.py` unchanged green; `test_package_hygiene.py` green with
  wheel+sdist (adds `docs/mcp-compatibility.md` to sdist assertion).

### S6 — Authority explainability
- Commit: `d37ec77`
- Files: `docs/operator-mode.md` (Flight Deck section + v0.7 surface
  manifest), per-tool docstrings carry authority class.
- Verified: per-tool authority tests across S3/S4/S5/S2 (owner gate for
  review+revoke, allowlist for events, policy gate for reconcile, audit for
  every call).

### S7 — Flight Deck UI presentation contract
- Commit: `d37ec77`
- Files: `docs/design/v0.7-flight-deck-ui-contract.md` (new), registered in
  `docs/design/README.md`.
- Change: read-only presentation contract (D2-A): data sources, no-new-
  authority rule, redaction, allowlist semantics, action-to-tool mapping,
  media conflicts F1–F7 resolved.

### S8 — Cross-machine seams (stretch, interfaces only)
- Commit: `44ddb40`
- Files: `seams.py` (new), `test_seams.py` (new), `pyproject.toml`
- Change: `DispatchAdapter` / `EvidenceProvider` Protocols + WorkOrder
  envelope; two-process-one-host fake over loopback validates the contract.
  No remote implementation exists or is promised.
- Verified: `test_seams.py` 5/5.

### S9 — Release hygiene
- Commit: `d37ec77`
- Files: `pyproject.toml` version → 0.7.0, `CHANGELOG.md` (0.7.0 entries),
  `README.md` (status + v0.7 summary), `docs/README.md` (version + map),
  `docs/release-notes-v0.7.0.md` (new), `docs/retention-policy.md` (new
  stores), `MANIFEST.in`, `operator_diagnostics.py` (release-doctor venv
  skip + source-module secret-substring exemption), `test_operator_diagnostics.py`
  (3 new doctor tests).
- Verified: `hermes_release_doctor` status WARN (blocking cleared; only
  "uncommitted changes" non-blocking), version 0.7.0; `test_package_hygiene.py`
  33/33; CI py_compile gate passes.

## Gate status

- G3 (slice implementation): implemented; S3/S4/S5 legal sign-off is a merge
  gate per legal memo (t_164cf992) — not yet recorded, pending default/Tony.
- G4 (independent verification): NOT executed — downstream task t_8e69d56b.
- G5 (public release): NOT executed — root card t_c1ef5a51.

## Verification commands (all run 2026-08-15)

```
env -u PYTHONPATH .venv/bin/python -m pytest            # 643 passed, 3 skipped
PYTHONPATH=/home/tony/.hermes/hermes-agent .venv/bin/python -m pytest test_operator_skills.py  # 34/34 (scoping test runs)
env -u PYTHONPATH .venv/bin/python -m pytest test_package_hygiene.py  # 33/33 (wheel+sdist CLEAN)
env -u PYTHONPATH .venv/bin/python -m py_compile server.py codex_core.py codex_mcp.py codex_config.py operator_codex.py updater.py operator_recovery.py operator_review.py operator_events.py operator_oauth.py token_store.py seams.py
PYTHONPATH=/home/tony/.hermes/hermes-agent .venv/bin/python -c "hermes_release_doctor"  # WARN, blocking []
server build_server() tool registration: all 6 new tools REGISTERED
```

## Commits (local, not pushed)

68e3397 S0 · 575b388 S1 · 6f49085 S2 · 1d94a9a S3 · d456eb9 S4 · f462dda S5 ·
44ddb40 S8 · d37ec77 S6/S7/S9

## Known gates / conditions

- S3/S4/S5 legal sign-off required before merge (t_164cf992 L3/L4/S1/S2/S3).
- `hermes_oauth_revoke` scope pending legal decision; documented in docs.
- No live ChatGPT connector verification was run (G4 task, requires live
  connector); OAuth promotion is code+doc complete but unverified live.
- Cross-machine execution intentionally unimplemented (NG1 / ADR-008).
