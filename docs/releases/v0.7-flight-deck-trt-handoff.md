# Hermes GPT v0.7.0 — TRT Technical Editorial Handoff (source pack)

- Task: t_8e69d56b → t_3fbfefff (Technical Editorial Planning)
- Date: 2026-08-15
- Status: implementation-truth verified (independent pass complete); **not
  published** — do not publish anything from this pack until G5.
- Purpose: give TRT the verified facts, evidence citations, and claim
  guardrails to plan review/news content for the v0.7.0 Flight Deck release.

---

## 1. What is verified true (cite these)

All facts below were re-verified against HEAD by the independent pass on
2026-08-15 (see `v0.7-flight-deck-verification-report.md`).

| Claim | Evidence | Status |
|---|---|---|
| Full test suite green: 646 collected, 0 failures, 3 skipped | `env -u PYTHONPATH .venv/bin/python -m pytest` (re-run) | SHIPPED |
| Review-accept writer `hermes_review_accept`: owner-gated, distinct reviewer enforced in code, bounded verdicts, evidence referenced not copied | `operator_review.py`, `test_operator_review.py` (12/12), live exercise | SHIPPED |
| Contract validator reads the review store as evidence | `operator_contract.py` lines 722-735 | SHIPPED |
| Event history `hermes_events_query`/`tail`: read-only, redacted, 5 sources, allowlist, retention window | `operator_events.py`, `test_operator_events.py` (12/12) | SHIPPED |
| Durable encrypted token storage: AES-256-GCM, 0600, restart reload, revoke+rotate | `token_store.py`, `test_token_store.py` (13/13), live exercise | SHIPPED (legal sign-off pending) |
| Restart reconciliation: blocked with `interrupted_by_restart`, never auto-advances, idempotent stage advance | `operator_recovery.py`, `test_operator_recovery.py` (7/7), live exercise | SHIPPED (tool-driven; no startup hook — D5) |
| MCP compatibility: floor 2024-11-05 → latest 2025-11-25, transports + auth metadata | `docs/mcp-compatibility.md`, `test_mcp_compat.py` (9/9), SDK 1.28.1 | SHIPPED |
| Cross-machine seam interfaces only (stretch), validated by two-process fake | `seams.py`, `test_seams.py` (5/5) | SHIPPED (stretch) |
| CI hermeticity fix; CI-like env green | S0; suite re-run | SHIPPED |
| Release doctor WARN-only, blocking cleared; package hygiene CLEAN (wheel+sdist) | re-run | SHIPPED |

## 2. What is NOT true / not yet done (do not claim)

- **v0.7.0 is not released on GitHub or PyPI.** GitHub latest is v0.6.0;
  PyPI is v0.5.0. Local commits only, not pushed. (README/docs currently
  overclaim — defect D2.)
- No live ChatGPT connector OAuth verification yet (G4 condition c).
- No legal sign-off recorded on token storage / retention / revoke scope
  (G4 condition d, legal memo S1/S2, L2/L3).
- No cross-machine remote execution — interfaces only.
- No public release action from any v0.7 card (root card constraint).

## 3. Claim guardrails (from legal memo + positioning memo)

- "Durable" — qualify with the key-file fallback on hosts without a keyring
  (this host is key-file).
- "Reviewed" — distinct-reviewer-profile, **not** independent audit (legal A1).
- Event history availability — `min(source retention, configured window)`, not
  a flat "90 days of history".
- OAuth compatibility — not claimable as shipped until G4 live verification.
- CI green — verified locally on 3.11; CI workflow matrix targets 3.10-3.12
  but has not run on these local commits.

## 4. Suggested editorial angles (TRT to develop)

1. **Durable, interactive, verifiable autonomy** — the thesis made concrete:
   review evidence, event history, restart recovery, encrypted tokens.
2. **Flight Deck as a control-plane theme** — no new authority, read-only
   presentation over the MCP surface (D2-A), wireframes in the design package.
3. **Restart-safe long-running work** — what survives a crash and what is
   operator-driven (honest framing: reconcile is a tool, not a daemon).
4. **Security posture** — local-first, loopback default, owner-gated
   mutations, no raw bodies anywhere, fail-closed reconciliation.
5. **What's gated** — the honest release story: code done, legal + live
   connector + release publishing still pending.

## 5. Source pack (files to cite)

- `docs/release-notes-v0.7.0.md` — user-facing release notes (fix count D3)
- `docs/releases/v0.7-flight-deck-verification-report.md` — independent verification
- `docs/releases/v0.7-flight-deck-defect-list.md` — recorded defects (D1-D8)
- `docs/releases/v0.7-flight-deck-demo-script.md` — runnable demo
- `docs/releases/v0.7-flight-deck-positioning-memo.md` — audience/message/proof points
- `docs/releases/v0.7-flight-deck-legal-risk-review.md` — claim guardrails
- `docs/design/v0.7-flight-deck-interaction-design.md` + wireframes — visuals
- `docs/mcp-compatibility.md`, `docs/oauth.md`, `docs/operator-mode.md` — current ops docs

## 6. Next step for TRT

Plan editorial coverage (review/news outline, claims backed by the verified
facts in §1, staging draft) with evidence citations. Do not publish. Route the
plan to Tony for approval; TRT card t_3fbfefff is the owner.
