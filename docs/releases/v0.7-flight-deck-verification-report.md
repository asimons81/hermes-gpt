# Hermes GPT v0.7.0 — Independent Verification Report (G4, code-level)

- Task: t_8e69d56b (Independent Verification and Docs/Demo Readiness)
- Date: 2026-08-15
- Verifier: default (independent of implementation owner t_d31703e4)
- Repository: /home/tony/hermes-gpt @ master (local commits d03a995; **not pushed**)
- Inputs verified: research package (t_78c0d90e), architecture + ADRs (t_5a3f6b5c),
  implementation plan (t_5a3f6b5c), legal risk review (t_164cf992),
  implementation evidence (t_d31703e4), current code + tests at HEAD

---

## 1. Scope and method

This is the independent verification pass for the implemented v0.7.0 Flight
Deck slices (S0–S9). It was run **fresh against HEAD** with the CI-like
environment (`env -u PYTHONPATH`), plus targeted live end-to-end exercises of
the restart-reconciliation, review-accept, event-history, and token-store
surfaces. Defects are recorded in the companion defect list
(`docs/releases/v0.7-flight-deck-defect-list.md`); **no defect was fixed in
this task**.

Verification covered the G4 exit criteria (research package §6, legal memo §7):

| G4 criterion | Status |
|---|---|
| (a) adversarial review tests pass (self-review rejected, distinct reviewer, bounded verdicts) | **PASS** — unit + live |
| (b) no-raw-body event tests pass across all five sources | **PASS** — unit + live |
| (c) live ChatGPT OAuth verification | **NOT EXECUTED** — requires live connector; see §5 |
| (d) legal sign-off on token storage + retention | **NOT RECORDED** — human/legal gate; see §5 |
| (e) full suite green | **PASS** — 646 collected, 0 failures, 3 skipped |
| (f) no regression in v0.6 safety tests | **PASS** — swarm/contract/oauth/skills suites green |

**Verdict: code-level G4 verification PASSES.** G4 as a release gate remains
**NOT-GO** only because (c) live ChatGPT connector verification and (d) legal
sign-off on token storage + retention are human/ops gates that cannot be
satisfied from this environment and are not yet recorded. G5 stays NOT-GO.

---

## 2. Suite results (independently re-run)

All commands run 2026-08-15 in `/home/tony/hermes-gpt`.

| Suite | Env | Result |
|---|---|---|
| Full suite | `env -u PYTHONPATH` | **646 collected, 0 failures, 3 skipped** (matches evidence "643 passed, 3 skipped") |
| `test_operator_skills.py` (S0) | no Hermes tree | 32 collected, 0 failures, **1 skipped** (scoping test) |
| `test_operator_skills.py` (S0) | with Hermes tree | 32 collected, 0 failures, 0 skipped |
| `test_mcp_compat.py` (S1) | no tree | 9/9 |
| `test_operator_recovery.py` (S2) | no tree | 7/7 |
| `test_operator_review.py` (S3) | no tree | 12/12 |
| `test_operator_events.py` (S4) | no tree | 12/12 |
| `test_token_store.py` (S5) | no tree | 13/13 |
| `test_seams.py` (S8) | no tree | 5/5 |
| `test_operator_diagnostics.py` (S9) | no tree | 28/28 |
| `test_operator_swarm.py` (regression) | no tree | green |
| `test_oauth_auth.py` (regression) | no tree | green |
| `test_operator_contract.py` (regression) | no tree | green |
| `test_package_hygiene.py` (S9) | no tree | 33/33 (wheel + sdist CLEAN) |
| py_compile (12 modules incl. new ones) | no tree | OK |

Release doctor (`hermes_release_doctor`): **status WARN, blocking_issues []**,
version 0.7.0, repo clean=false (uncommitted changes — non-blocking).

Tool registration (`build_server()._tool_manager`): 88 tools total; all 8
v0.7-touched tools registered:
`hermes_operator_recover`, `hermes_swarm_reconcile`, `hermes_swarm_stage_advance`,
`hermes_review_accept`, `hermes_events_query`, `hermes_events_tail`,
`hermes_oauth_status`, `hermes_oauth_revoke`.

---

## 3. Slice-by-slice verification

### S0 — CI hermeticity fix (68e3397)
Code verified: `operator_skills._call_skill_manager` wraps the
`hermes_constants` import in try/except (operator_skills.py:218-240); on
unavailable import, scoping is a no-op for the default profile (profile_home
== hermes_root) and fails closed for non-default profiles. Previously 8
failures on CI-like env; now green with 1 skip. **PASS.**

### S1 — MCP compatibility manifest (575b388)
`docs/mcp-compatibility.md` pins floor 2024-11-05 through SDK latest
2025-11-25 with transport + auth metadata; `test_mcp_compat.py` asserts the
installed SDK's `SUPPORTED_PROTOCOL_VERSIONS` (verified against mcp 1.28.1 in
venv) and tool metadata. 9/9 green. **PASS.**

### S2 — Restart reconciliation + idempotent advance (6f49085)
- Live exercise: workflow with stage in `running` → dry-run reconcile reports
  interrupted=1 and does **not** mutate → apply reconcile marks stage
  `blocked` with `reason: interrupted_by_restart` and workflow `blocked` →
  token reload reports `integrity: EMPTY` (no envelope) → re-advance of a
  done stage returns `idempotent: true, changed: false`.
- Never-auto-advance verified (test + code: `_reconcile_swarm_stages` only
  writes blocked status; no dispatch/advance call path).
- **Defect D5** (see list): ADR-007 says recovery "runs on startup" but the
  implementation is tool-only — no startup hook exists in server.py. After a
  restart, an operator must call `hermes_swarm_reconcile` explicitly. Fail-closed
  property is preserved; the automatic-hook claim is unimplemented.
- **PASS with documented deviation.**

### S3 — Review-accept writer (1d94a9a)
- Live exercise: `hermes_review_accept` dry-run writes nothing; apply writes a
  `hermes.review-acceptance/v1` JSONL record (0600) at
  `review-evidence/review-acceptances.jsonl`; self-review rejected
  (`INVALID_REVIEW_ACCEPTANCE`); verdict outside `SATISFIED`/`NOT_SATISFIED`
  rejected.
- Legal L4 verified: `_validate_refs` rejects denied paths via
  `op.is_denied_path` (operator_review.py:65-76).
- `operator_contract._check_review` reads the store as evidence 1b
  (operator_contract.py:722-735).
- **PASS.** (Legal sign-off still pending — gate, not code defect.)

### S4 — Structured event history (d456eb9)
- Live exercise: seeded audit/swarm/codex/cron/kanban rows; `hermes_events_query`
  and `hermes_events_tail` return newest-first ordered events; allowlist
  `HERMES_GPT_EVENTS_ALLOWED_SOURCES=kanban` restricts; no store writes.
- No-raw-body test across all five sources (test_operator_events.py
  `test_no_raw_body_across_all_sources`); refs are logical namespaced refs
  (`swarm:`, `codex:`, `cron:`, `kanban:`), never filesystem paths.
- Retention filter, caps/truncation, read-only-by-construction, per-call
  audit all verified. **PASS.**

### S5 — Durable token storage + OAuth promotion (f462dda)
- Live exercise: `save_tokens` writes AES-256-GCM envelope
  `secrets/hermes_gpt_tokens.json` (0600) + key file (0600); no plaintext
  token material on disk; reload roundtrip returns tokens; corrupt envelope
  fails closed (`TokenStoreError`); `revoke_tokens` deletes envelope and
  rotates key.
- **Observation D6**: on this host no keyring service is available, so the key
  source resolves to `keyfile` — the documented "materially weaker" fallback
  (legal S1). This is the operative posture on this machine and requires the
  pending legal sign-off, not a code change.
- deps verified: `cryptography>=42` required, `keyring` in dev extras.
- **PASS.** (Legal sign-off + live-connector verification still pending.)

### S6 — Authority explainability (d37ec77)
`docs/operator-mode.md` Flight Deck section + v0.7 surface manifest
(operator-mode.md:244-308) list each tool with authority class, gates, audit,
allowlist; matches registered tool authorities verified in code. **PASS.**

### S7 — Flight Deck UI presentation contract (d37ec77)
`docs/design/v0.7-flight-deck-ui-contract.md` exists and is registered;
interaction design + 6 SVG wireframes from media lane (61e2840) are present
and QA'd by the media lane. No new authority introduced. **PASS.**

### S8 — Cross-machine seams (44ddb40, stretch)
`seams.py` defines `DispatchAdapter` / `EvidenceProvider` Protocols + WorkOrder
envelope; `test_seams.py` 5/5 (two-process-one-host fake over loopback). No
remote implementation promised. **PASS.**

### S9 — Release hygiene (d37ec77)
Version 0.7.0 in pyproject.toml; CHANGELOG 0.7.0 entries; README/docs map
updated; release notes present; retention policy updated with L2/L3 legal
findings. **PASS**, with doc defects D1/D2/D3 (see list).

---

## 4. Production evidence review

`docs/releases/v0.7-flight-deck-implementation-evidence.md` (d03a995) was
reviewed claim-by-claim against HEAD:

- Slice file lists, commits, and tool registrations: **accurate**.
- Suite counts: **substantially accurate** (646 vs "643 passed + 3 skipped" —
  the 643/3 figure is the pass/skip split; the evidence's own S0 claim of
  "34/34" is wrong — see D1).
- Gate status (G3 implemented/legal-pending, G4 downstream, G5 not executed):
  **accurate**.
- Verification commands in the evidence reproduce: **yes** (all re-run green).

---

## 5. G4 gate conditions and blockers (not code defects)

| Condition | Status | Owner / next action |
|---|---|---|
| (c) live ChatGPT connector OAuth verification | **NOT EXECUTED** — needs the live ChatGPT connector deployment (`docs/windows-chatgpt-codex.md`) and a live OAuth handshake; not possible from this headless env | Tony / legal S3 sign-off first |
| (d) legal sign-off on token storage + retention windows (S1/S2, L2/L3) | **NOT RECORDED** — legal memo t_164cf992 sets conditions; token key-file fallback is the operative posture on this host | legal + Tony |
| `hermes_oauth_revoke` scope decision | pending legal (documented as pending in docs) | legal |
| G5 (release) | NOT-GO until G4 conditions met + P0-4 counsel checkpoint + release checklist + separate GitHub/PyPI | Tony |

---

## 6. Success criteria mapping (research §5.3)

| Criterion | Status |
|---|---|
| S1 restart mid-flight, resume from durable state without losing evidence | **PASS** via explicit reconcile (D5 notes it is tool-driven, not automatic) |
| S2 distinct reviewer writes acceptance; validate SATISFIED only with evidence; self-review rejected | **PASS** |
| S3 structured event history without raw bodies | **PASS** |
| S4 OAuth boundary released/tested/documented; no auth-less remote; restart behavior documented | **PARTIAL** — code+docs done; live ChatGPT verification pending (G4c) |
| S5 every new mutation audited, gated, mapped to existing authority; no v0.6 regression | **PASS** |
| S6 CI green; release doctor PASS/WARN-only | **PASS** (doctor WARN-only; full suite green on 3.11; CI workflow matrix 3.10-3.12 exists but not live-run on these commits) |

---

## 7. Conclusion

The v0.7.0 Flight Deck slices are implemented as claimed and pass independent
code-level verification: full suite green, adversarial review and no-raw-body
event controls hold, restart reconciliation and token storage behave
fail-closed end-to-end, and the docs/demo package is ready. G4 is **PASS at
the code level** and **NOT-GO as a release gate** pending (c) live ChatGPT
connector verification and (d) legal sign-off — both are human/ops gates, not
implementation defects. G5 remains NOT-GO. See the defect list for the
recorded issues (none blocking at code level; two are doc-accuracy, one is a
CI gate list gap, one is an ADR-vs-implementation startup-hook deviation).
