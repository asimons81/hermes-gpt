# Hermes GPT v0.7.0 — Handoff for Tony (approval gate summary)

- Task: t_8e69d56b (Independent Verification and Docs/Demo Readiness)
- Date: 2026-08-15
- Repo: /home/tony/hermes-gpt @ master, local commits d03a995 (**not pushed**)

---

## Bottom line

The v0.7.0 Flight Deck slices (S0–S9) are implemented and pass independent
code-level verification: **full suite green (646 collected, 0 failures, 3
skipped)**, adversarial review controls hold, event surface is no-raw-body,
token storage and restart reconciliation behave fail-closed end-to-end, and
release doctor is WARN-only with no blocking issues.

**G4 is PASS at the code level but NOT-GO as a release gate** until two
human/ops items are done:
1. **Live ChatGPT connector OAuth verification** (needs the deployed
   connector + legal sign-off first — cannot be run headless).
2. **Legal sign-off recorded** on token storage, retention windows, and the
   `hermes_oauth_revoke` scope (legal memo t_164cf992 conditions).

**G5 (public release) remains NOT-GO.** v0.7.0 is local-only; GitHub latest is
still v0.6.0, PyPI v0.5.0.

---

## What was verified (independent pass)

- All 8 v0.7-touched tools registered (`hermes_review_accept`,
  `hermes_events_query/tail`, `hermes_oauth_status/revoke`,
  `hermes_swarm_reconcile`, `hermes_operator_recover`,
  `hermes_swarm_stage_advance`).
- Live end-to-end: restart reconcile (dry-run → apply → blocked with
  `interrupted_by_restart` → idempotent re-advance); review accept
  (self-review rejected, bounded verdicts); events query/tail (ordering,
  allowlist, read-only); token store (AES-256-GCM, 0600, no plaintext on
  disk, corrupt fails closed, revoke rotates key).
- Docs/release notes/surface manifest/UI contract/retention policy all
  present and mostly accurate; legal L2 (min(source, window)) and L3
  (review-evidence retention) incorporated into retention policy.

## Defects recorded (NOT fixed — follow-up cards)

- **D2 (needs fixing before G5):** README + docs/README claim "GitHub release
  v0.7.0" — that is false; latest release is v0.6.0. Must be corrected before
  any public claim.
- **D5 (decision needed):** ADR-007 promised a startup hook for restart
  reconciliation, but recovery is tool-only (`hermes_swarm_reconcile`). Safe
  (never auto-advances) but operator-driven. Decide: accept and amend ADR, or
  add a dry-run startup hook.
- **D6:** on this host the token key falls back to key-file (no keyring) — the
  documented weaker path; legal sign-off is a real gate here, consider
  installing a keyring backend.
- D1/D3/D4: doc count accuracy + CI py_compile list hygiene (P3).

Full list: `docs/releases/v0.7-flight-deck-defect-list.md` (D1–D8, no P1).

## What needs you / legal

1. Legal sign-off on token storage + retention + revoke scope (S3/S4/S5).
2. Schedule live ChatGPT connector verification (G4c) with the deployed
   connector.
3. Decide D5 (accept tool-driven recovery or add startup hook).
4. Approve correcting the false "GitHub release v0.7.0" doc claims (D2).
5. G5 release checklist + separate GitHub/PyPI publishing — only after the
   above.

## Deliverables from this task

- `docs/releases/v0.7-flight-deck-verification-report.md`
- `docs/releases/v0.7-flight-deck-defect-list.md`
- `docs/releases/v0.7-flight-deck-demo-script.md`
- `docs/releases/v0.7-flight-deck-trt-handoff.md` (feeds TRT editorial planning)
- Existing: `docs/release-notes-v0.7.0.md` (user-facing; fix D3 count)

TRT editorial planning (t_3fbfefff) is unblocked and waiting on this task's
completion.
